Labels :         Frames :

    window
    curson("arrow","circle","clock","heart","spider","star","cross")
    text
    foreground
    background
    font='myfont4.ttf 15 bold italic overstrike underline'
    padx(int)
    pady(int)
    relief(RAISED,FLAT,SUNKEN,RIDGE,GROOVE,ITALIC)
    border(int)
    borderwidth(int)
    compound(left,right,top,bottom)
    image(path)
    justify(right or left) -> only
    underline
    width

    state(disabled or normol)
    grid(row,column)
    place(x_cordinate,y_cordinate)# same python cordinates
    pack(side,anchor,expand)